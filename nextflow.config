process {
    executor = 'slurm'
    queue = 'research'
    cpus = 1
    memory = '16 GB'

    maxForks = 300
}

executor {
    queueSize = 300
    submitRateLimit = '3/1s'

    retry {
        delay = '10s'
        maxAttempt = 5
    }
}
